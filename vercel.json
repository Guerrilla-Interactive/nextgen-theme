{
  "buildCommand": "npx playwright install chromium --force || echo 'Using screenshot fallback' && next build",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "functions": {
    "app/**/*.ts": {
      "memory": 3009,
      "maxDuration": 120
    }
  },
  "env": {
    "PLAYWRIGHT_BROWSERS_PATH": "0"
  }
} 